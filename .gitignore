# Dependencias (NO subir)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Archivos de sistema (NO subir)
.DS_Store
Thumbs.db
desktop.ini

# IDE (NO subir)
.vscode/
.idea/
*.swp
*.swo

# Variables de entorno (NO subir - secretos)
.env
.env.local
.env.production

# Logs (NO subir)
logs/
*.log

# Build temporal (NO subir)
dist/
build/
.cache/

# Pero SÍ subir esto (lo que Git ya tenía antes)
# Nota: Estos archivos ya están en el repo, no se pierden